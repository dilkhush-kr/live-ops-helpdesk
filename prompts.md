# Prompts.md

* Why socket events were firing twice in development mode
* How to properly clean up socket listeners using `socket.off()`
* Understanding `useEffect` cleanup in realtime applications

* Understanding how frontend and backend communicate using Socket.io
* Structuring realtime events like:

  * `lock_ticket`
  * `unlock_ticket`
  * `create_ticket`


* Understanding how to use JavaScript `Map()` for ticket locking
* Associating locked tickets with socket IDs
* Preventing multiple agents from editing the same ticket

* Handling unexpected socket disconnects
* Automatically unlocking tickets on disconnect
* Showing reconnect warning banners in frontend UI




